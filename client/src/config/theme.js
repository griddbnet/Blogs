import { createTheme } from "@mui/material";

const theme = createTheme({
  palette: {
    gradient: {
      main: "linear-gradient(94deg, #8DC63F 15.23%, #7FC520 99.56%);",
    },
  },
});

export default theme;
